#include "us4/runtime/benchmarks/benchmark_registry.h"

namespace us4::runtime::benchmarks
{

    std::vector<BenchmarkCase> BenchmarkRegistry::DefaultCases()
    {
        return {
            BenchmarkCase{
                .name = "scalar_matmul_reference",
                .backend = "cpu",
                .scenario = "fp32-naive-gemm",
                .modelId = "synthetic-dense",
                .adapterId = "dense-reference",
                .profileId = "cpu-only",
                .runtimeMode = "CPU_ONLY",
                .promptTokens = 128,
                .generationTokens = 0,
                .requiresGpu = false,
                .participatesInCorrectnessGate = true,
                .touchesCli = false,
            },
            BenchmarkCase{
                .name = "scalar_attention_reference",
                .backend = "cpu",
                .scenario = "causal-softmax-kv-concat",
                .modelId = "synthetic-dense",
                .adapterId = "dense-reference",
                .profileId = "cpu-only",
                .runtimeMode = "CPU_ONLY",
                .promptTokens = 128,
                .generationTokens = 32,
                .requiresGpu = false,
                .participatesInCorrectnessGate = true,
                .touchesCli = false,
            },
            BenchmarkCase{
                .name = "dense_baseline_qwen_cpu_only",
                .backend = "cpu",
                .scenario = "prefill-decode-dense",
                .modelId = "qwen-0.5b",
                .adapterId = "dense-qwen",
                .profileId = "cpu-only",
                .runtimeMode = "CPU_ONLY",
                .promptTokens = 128,
                .generationTokens = 32,
                .requiresGpu = false,
                .participatesInCorrectnessGate = true,
                .touchesCli = true,
            },
            BenchmarkCase{
                .name = "dense_baseline_gemma_cpu_only",
                .backend = "cpu",
                .scenario = "prefill-decode-dense",
                .modelId = "gemma-2b",
                .adapterId = "dense-gemma",
                .profileId = "cpu-only",
                .runtimeMode = "CPU_ONLY",
                .promptTokens = 128,
                .generationTokens = 32,
                .requiresGpu = false,
                .participatesInCorrectnessGate = true,
                .touchesCli = true,
            },
            BenchmarkCase{
                .name = "dense_baseline_llama_cpu_only",
                .backend = "cpu",
                .scenario = "prefill-decode-gqa",
                .modelId = "llama-3.2-3b",
                .adapterId = "dense-llama",
                .profileId = "cpu-only",
                .runtimeMode = "CPU_ONLY",
                .promptTokens = 128,
                .generationTokens = 32,
                .requiresGpu = false,
                .participatesInCorrectnessGate = true,
                .touchesCli = true,
            },
            BenchmarkCase{
                .name = "llama_8b_cpu_avx_contract",
                .backend = "cpu",
                .scenario = "llama-8b-avx-contract",
                .modelId = "llama-3.1-8b",
                .adapterId = "dense-llama",
                .profileId = "cpu-only",
                .runtimeMode = "CPU_ONLY",
                .promptTokens = 256,
                .generationTokens = 64,
                .requiresGpu = false,
                .participatesInCorrectnessGate = false,
                .touchesCli = false,
            },
            BenchmarkCase{
                .name = "llama_8b_cuda_contract",
                .backend = "cuda",
                .scenario = "llama-8b-cuda-reference-contract",
                .modelId = "llama-3.1-8b",
                .adapterId = "dense-llama",
                .profileId = "full",
                .runtimeMode = "FULL",
                .promptTokens = 256,
                .generationTokens = 64,
                .requiresGpu = true,
                .participatesInCorrectnessGate = false,
                .touchesCli = false,
            },
            BenchmarkCase{
                .name = "llama_8b_directml_contract",
                .backend = "directml",
                .scenario = "llama-8b-directml-graph-contract",
                .modelId = "llama-3.1-8b",
                .adapterId = "dense-llama",
                .profileId = "balanced",
                .runtimeMode = "BALANCED",
                .promptTokens = 256,
                .generationTokens = 64,
                .requiresGpu = true,
                .participatesInCorrectnessGate = false,
                .touchesCli = false,
            },
            BenchmarkCase{
                .name = "dense_baseline_bitnet_cpu_only",
                .backend = "cpu",
                .scenario = "prefill-decode-bitnet-reference",
                .modelId = "bitnet-b1.58",
                .adapterId = "dense-bitnet",
                .profileId = "nano",
                .runtimeMode = "NANO",
                .promptTokens = 128,
                .generationTokens = 32,
                .requiresGpu = false,
                .participatesInCorrectnessGate = true,
                .touchesCli = true,
            },
            BenchmarkCase{
                .name = "dense_baseline_ternary_cpu_only",
                .backend = "cpu",
                .scenario = "prefill-decode-ternary-reference",
                .modelId = "ternary-int4",
                .adapterId = "dense-ternary",
                .profileId = "nano",
                .runtimeMode = "NANO",
                .promptTokens = 128,
                .generationTokens = 32,
                .requiresGpu = false,
                .participatesInCorrectnessGate = true,
                .touchesCli = true,
            },
            BenchmarkCase{
                .name = "vulkan_qwen_balanced",
                .backend = "vulkan",
                .scenario = "cross-vendor-dense-prefill-decode",
                .modelId = "qwen-0.5b",
                .adapterId = "dense-qwen",
                .profileId = "balanced",
                .runtimeMode = "BALANCED",
                .promptTokens = 256,
                .generationTokens = 32,
                .requiresGpu = true,
                .participatesInCorrectnessGate = true,
                .touchesCli = true,
            },
            BenchmarkCase{
                .name = "vulkan_llama_balanced",
                .backend = "vulkan",
                .scenario = "cross-vendor-gqa-prefill-decode",
                .modelId = "llama-3.2-3b",
                .adapterId = "dense-llama",
                .profileId = "balanced",
                .runtimeMode = "BALANCED",
                .promptTokens = 256,
                .generationTokens = 32,
                .requiresGpu = true,
                .participatesInCorrectnessGate = true,
                .touchesCli = true,
            },
            BenchmarkCase{
                .name = "windows_ml_qwen_opt_in",
                .backend = "windows-ml",
                .scenario = "npu-dense-offload-opt-in",
                .modelId = "qwen-0.5b",
                .adapterId = "dense-qwen",
                .profileId = "micro",
                .runtimeMode = "MICRO",
                .promptTokens = 256,
                .generationTokens = 32,
                .requiresGpu = false,
                .participatesInCorrectnessGate = true,
                .touchesCli = true,
            },
            BenchmarkCase{
                .name = "windows_ml_llama_opt_in",
                .backend = "windows-ml",
                .scenario = "npu-gqa-offload-opt-in",
                .modelId = "llama-3.2-3b",
                .adapterId = "dense-llama",
                .profileId = "micro",
                .runtimeMode = "MICRO",
                .promptTokens = 256,
                .generationTokens = 32,
                .requiresGpu = false,
                .participatesInCorrectnessGate = true,
                .touchesCli = true,
            },
            BenchmarkCase{
                .name = "windows_ml_qwen_thermal_throttle",
                .backend = "windows-ml",
                .scenario = "npu-dense-offload-thermal-throttle",
                .modelId = "qwen-0.5b",
                .adapterId = "dense-qwen",
                .profileId = "micro",
                .runtimeMode = "MICRO",
                .promptTokens = 256,
                .generationTokens = 32,
                .requiresGpu = false,
                .participatesInCorrectnessGate = true,
                .touchesCli = true,
            },
            BenchmarkCase{
                .name = "windows_ml_qwen_no_npu_fallback",
                .backend = "windows-ml",
                .scenario = "npu-opt-in-cpu-fallback-no-device",
                .modelId = "qwen-0.5b",
                .adapterId = "dense-qwen",
                .profileId = "micro",
                .runtimeMode = "MICRO",
                .promptTokens = 256,
                .generationTokens = 32,
                .requiresGpu = false,
                .participatesInCorrectnessGate = true,
                .touchesCli = true,
            },
            BenchmarkCase{
                .name = "moe_kimi_cpu_only",
                .backend = "cpu",
                .scenario = "expert-routing-load-balance",
                .modelId = "kimi-k2",
                .adapterId = "moe-kimi",
                .profileId = "degraded",
                .runtimeMode = "CPU_ONLY",
                .promptTokens = 256,
                .generationTokens = 32,
                .requiresGpu = false,
                .participatesInCorrectnessGate = true,
                .touchesCli = true,
            },
            BenchmarkCase{
                .name = "dense_baseline",
                .backend = "cpu",
                .scenario = "single-batch-dense",
                .modelId = "qwen-0.5b",
                .adapterId = "dense-qwen",
                .profileId = "cpu-only",
                .runtimeMode = "CPU_ONLY",
                .promptTokens = 128,
                .generationTokens = 32,
                .requiresGpu = false,
                .participatesInCorrectnessGate = true,
                .touchesCli = true,
            },
            BenchmarkCase{
                .name = "moe_throughput",
                .backend = "cuda",
                .scenario = "expert-routing-throughput",
                .modelId = "deepseek-moe",
                .adapterId = "moe-deepseek",
                .profileId = "full",
                .runtimeMode = "FULL",
                .promptTokens = 512,
                .generationTokens = 64,
                .requiresGpu = true,
                .participatesInCorrectnessGate = false,
                .touchesCli = false,
            },
            BenchmarkCase{
                .name = "correctness_diff",
                .backend = "cross-backend",
                .scenario = "logit-delta-gate",
                .modelId = "qwen-0.5b",
                .adapterId = "dense-qwen",
                .profileId = "cpu-only",
                .runtimeMode = "CPU_ONLY",
                .promptTokens = 32,
                .generationTokens = 32,
                .requiresGpu = false,
                .participatesInCorrectnessGate = true,
                .touchesCli = true,
            },
        };
    }

    std::vector<BenchmarkCase> BenchmarkRegistry::CasesForBackend(const std::string& backend)
    {
        std::vector<BenchmarkCase> cases;
        for (const BenchmarkCase& benchmark : DefaultCases())
        {
            if (benchmark.backend == backend || benchmark.backend == "cross-backend")
            {
                cases.push_back(benchmark);
            }
        }
        return cases;
    }

    std::optional<BenchmarkCase> BenchmarkRegistry::FindByName(const std::string& name)
    {
        for (const BenchmarkCase& benchmark : DefaultCases())
        {
            if (benchmark.name == name)
            {
                return benchmark;
            }
        }
        return std::nullopt;
    }

} // namespace us4::runtime::benchmarks
